import 'package:flutter/material.dart';

class CancelGranularEquipmentOwner extends StatelessWidget {
  const CancelGranularEquipmentOwner({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('设置'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('设置界面'),
      ),
    );
  }
}
